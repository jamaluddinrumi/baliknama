{
  "private": true,
  "scripts": {
    "build": "nuxi build",
    "dev": "nuxi dev --https --ssl-cert localhost.pem --ssl-key localhost-key.pem",
    "generate": "nuxi generate",
    "preview": "nuxi preview"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^5.1.2",
    "nuxt": "3.0.0-rc.3",
    "sass": "^1.52.2"
  },
  "dependencies": {
    "@inkline/inkline": "^3.0.8",
    "@inkline/nuxt": "^3.1.0"
  }
}
